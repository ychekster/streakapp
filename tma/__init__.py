"""Telegram Mini App (TMA) StreakApp.

Содержит API-сервер (`tma/backend`), который владеет базой данных, и фронтенд
(`tma/frontend`). Бот (`bot/`) открывает приложение и присылает напоминания о
привычках — их он только читает через слой данных `tma/backend`.
"""
